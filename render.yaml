# render.yaml

databases:
  - name: mybbs_db
    databaseName: mybbs
    user: mybbs_user
    plan: free

services:
  - type: web
    name: my-flask-bbs
    env: python
    # ★★★ ここに閉じクォーテーションを追加してください ★★★
    buildCommand: "pip install -r requirements.txt && python app_10.py db_init"
    startCommand: "gunicorn app_10:app"
    plan: free
    healthCheckPath: /
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: DATABASE_URL
        fromDatabase:
          name: mybbs_db
          property: connectionString