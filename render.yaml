# render.yaml

databases:
  - name: mybbs_db
    databaseName: mybbs
    user: mybbs_user
    plan: free

services:
  - type: web
    name: my-flask-bbs
    env: python
    # ★★★ ここに閉じクォーテーションを追加してください ★★★
    # render.yaml

services:
  # ...
  - type: web
    name: my-flask-bbs
    env: python
    # ★★★ この行を以下の新しいコマンドに置き換えます ★★★
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn app_10:app"
    # ...
    plan: free
    healthCheckPath: /
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: DATABASE_URL
        fromDatabase:
          name: mybbs_db
          property: connectionString